import { DATA_COLOR_PALETTE, UNIVERSE_SCALE } from '../constant'

export const particleCount = 2000
export const radiusLimit = UNIVERSE_SCALE * 4
export const colorsArray = Object.values(DATA_COLOR_PALETTE).map((color) => color)
